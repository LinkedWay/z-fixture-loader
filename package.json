{
  "name": "fixture_loader",
  "version": "1.0.0",
  "description": "load fixuture from file system to various datasouce such as elasticesearch, mysql db, redis..",
  "main": "index.js",
  "scripts": {
    "test": "docker-compose run app ./node_modules/.bin/mocha test"
  },
  "author": "Ray Zhang",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "elasticsearch": "^10.1.3"
  }
}
